spring:
  application:
    name: TasteOfSeoul
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://rave-dev.ddns.net:8080/test
    username: root
    password: R0JQZVpNYkIzSA==
  jpa:
    #    hibernate:
    #      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MariaDB106Dialect